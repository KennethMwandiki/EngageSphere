{
  "name": "engagesphere",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/KennethMwandiki/EngageSphere.git"
  },
  "scripts": {
  "build:web": "echo \"Building web app...\" && mkdir -p dist && cp -r frontend-prototype.html styles.css dist/ || true",
  "build": "node ./scripts/build-web.js",
    "build:android": "echo \"Android build placeholder. Run your native build here (e.g., Gradle)\" && exit 0",
    "build:ios": "echo \"iOS build placeholder. Requires macOS runner with Xcode\" && exit 0",
    "test": "echo \"No tests configured.\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.5.0"
  }
}
